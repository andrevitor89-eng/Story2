# Copie para backend/.env e preencha se quiser geração real com Gemini.
# Sem GEMINI_API_KEY o worker usa placeholders (offline).

JWT_SECRET=change-me-in-production
GEMINI_API_KEY=
OFFLINE_FALLBACK=true
